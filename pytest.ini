[pytest]
# Discover the complete repository test suite under tests/. Vendored and
# generated dependency trees remain excluded below so CI does not execute
# unrelated third-party tests.
testpaths = tests
python_files = test_*.py
norecursedirs = .git .venv llama.cpp android navigation backups
